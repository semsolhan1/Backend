package com.handifarm.api.snsBoard.repository;

import com.handifarm.api.snsBoard.entity.SnsHashTag;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SnsHashTagRepository extends JpaRepository<SnsHashTag, Long> {
}
